<!-- 地址编辑页面 -->
<view class="address-edit-container">
  <!-- 遮罩层 -->
  <view class="page-mask" wx:if="{{showRegionPicker}}" bindtap="onCloseRegionPicker"></view>

  <!-- 表单弹窗 -->
  <view class="form-sheet">
    <!-- 手柄 + 标题 -->
    <view class="sheet-header">
      <view class="sheet-handle"></view>
      <view class="sheet-title-wrap">
        <text class="sheet-title">{{mode === 'edit' ? '编辑地址' : '新增地址'}}</text>
        <view class="close-btn" bindtap="onBackTap">
          <text class="close-icon">✕</text>
        </view>
      </view>
      <view class="sheet-divider"></view>
    </view>

    <!-- 表单内容 -->
    <scroll-view class="form-body" scroll-y>
      <!-- 收货人 -->
      <view class="form-item">
        <label class="form-label">
          收货人 <text class="required">*</text>
        </label>
        <view class="input-wrap {{errors.name ? 'error' : ''}}">
          <text class="input-icon">👤</text>
          <input
            class="form-input"
            type="text"
            value="{{form.name}}"
            placeholder="请输入收货人姓名"
            data-field="name"
            bindinput="onInputChange"
          />
        </view>
        <text class="error-text" wx:if="{{errors.name}}">⚠ {{errors.name}}</text>
      </view>

      <!-- 手机号 -->
      <view class="form-item">
        <label class="form-label">
          手机号 <text class="required">*</text>
        </label>
        <view class="input-wrap {{errors.phone ? 'error' : ''}}">
          <text class="input-icon">📱</text>
          <input
            class="form-input"
            type="number"
            value="{{form.phone}}"
            placeholder="请输入11位手机号"
            maxlength="11"
            data-field="phone"
            bindinput="onInputChange"
          />
        </view>
        <text class="error-text" wx:if="{{errors.phone}}">⚠ {{errors.phone}}</text>
      </view>

      <!-- 所在地区 (已隐藏) -->
      <!--
      <view class="form-item">
        <label class="form-label">
          所在地区 <text class="required">*</text>
        </label>
        <view class="input-wrap {{errors.province ? 'error' : ''}}" bindtap="onShowRegionPicker">
          <text class="input-icon">📍</text>
          <text class="region-text {{form.province ? '' : 'placeholder'}}">
            {{form.province ? form.province + ' ' + form.city + ' ' + form.district : '请选择省/市/区'}}
          </text>
          <text class="arrow-icon">›</text>
        </view>
        <text class="error-text" wx:if="{{errors.province}}">⚠ {{errors.province}}</text>
      </view>
      -->

      <!-- 详细地址 -->
      <view class="form-item">
        <label class="form-label">
          详细地址 <text class="required">*</text>
        </label>
        <view class="textarea-wrap {{errors.detail ? 'error' : ''}}">
          <textarea
            class="form-textarea"
            value="{{form.detail}}"
            placeholder="请输入街道、楼号、房间号等详细信息"
            rows="3"
            data-field="detail"
            bindinput="onInputChange"
          />
        </view>
        <text class="error-text" wx:if="{{errors.detail}}">⚠ {{errors.detail}}</text>
      </view>

      <!-- 设为默认 -->
      <view class="form-item switch-item" bindtap="onDefaultToggle">
        <text class="form-label">设为默认地址</text>
        <view class="switch-wrap">
          <view class="custom-switch {{form.isDefault ? 'on' : ''}}">
            <view class="switch-thumb"></view>
          </view>
        </view>
      </view>
    </scroll-view>

    <!-- 保存按钮 -->
    <view class="form-footer">
      <view class="save-btn" bindtap="onSave">保存地址</view>
    </view>
  </view>

  <!-- 地区选择器（全屏）- 已隐藏 -->
  <!--
  <view class="region-picker" wx:if="{{showRegionPicker}}">
    ...
  </view>
  -->
</view>
